
var chai = require("chai");

var assert = chai.assert;


it("renders without crashing", () => {
  assert.equal(true, true);
});

it("renders without crashing", () => {
  assert.equal(true, true);
});

it("renders without crashing", () => {
  assert.equal(true, true);
});

it("renders without crashing", () => {
  assert.equal(true, true);
});

it("renders without crashing", () => {
  assert.equal(true, true);
});

it("renders without crashing", () => {
  assert.equal(true, true);
});

it("renders without crashing", () => {
  assert.equal(true, true);
});
